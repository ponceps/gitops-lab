apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# prod environment
namespace: prod
resources:
  - ../../base
# Kargo promotion updates newTag here as Freight flows into this stage.
images:
  - name: nginx
    newTag: 1.27.3
